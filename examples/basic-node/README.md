# basic-node example

A minimal Node.js script showing the core `add` / `search` flow with OpenAI embeddings and PGlite storage.

```bash
export OPENAI_API_KEY=sk-...
pnpm --filter @turbomem/example-basic-node start
```
