---
"@turbomem/mcp": minor
---

Add `@turbomem/mcp`: a Model Context Protocol server that gives Claude Desktop a private, local long-term memory. Ships as a one-click `.mcpb` extension with `remember`, `recall`, `list_memories`, `forget`, and `forget_everything` tools, and supports OpenAI, Google, and Anthropic providers via a single API key.
