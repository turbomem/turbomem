---
"turbomem": minor
---

Add optional sqlite-vec storage backend (`storage: "sqlite-vec"`) alongside the default PGlite backend. Requires optional peer dependencies `better-sqlite3` and `sqlite-vec`.
