# Embed README.md into each package's published manifest so npmjs.com renders it.
# pnpm defaults this to false, which leaves the registry "readme" field empty
# even though README.md is included in the tarball.
embed-readme=true
